{
  "name": "av-hacks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ av-hacks",
    "create-local": "graph create --node http://localhost:8020/ av-hacks",
    "remove-local": "graph remove --node http://localhost:8020/ av-hacks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 av-hacks"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
